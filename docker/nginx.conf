# Local dev nginx config — no CORS headers (same-origin only).
# For Docker deployments with cross-origin needs, use nginx.conf.template
# with the CORS_ORIGINS env var (see docs/DEPLOYMENT.md).
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Editor bundle — full authoring app
    # Future: add auth_basic block here to gate access
    location /editor/ {
        try_files $uri $uri/ /editor/index.html;
    }

    # Enable gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/wasm;

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|wasm)$ {
        expires 1d;
        add_header Cache-Control "public, immutable";
    }

    # Block direct access to backup history files (must use authenticated API)
    location ^~ /archives/history/ {
        deny all;
        return 403;
    }

    # Serve archive files (.ddim, .a3d, .a3z) with proper headers
    location ~* \.(ddim|a3d|a3z)$ {
        expires 1d;
        add_header Cache-Control "public";
        # Serve as application/zip for proper handling
        types {
            application/zip ddim a3d a3z;
        }
    }

    # Serve E57 point cloud files with proper headers
    location ~* \.e57$ {
        expires 1d;
        add_header Cache-Control "public";
        types {
            application/octet-stream e57;
        }
    }

    # CSP frame-ancestors (prevents clickjacking) - must be set via HTTP headers, not meta tags
    add_header Content-Security-Policy "frame-ancestors 'self'" always;

    # Additional security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
}
